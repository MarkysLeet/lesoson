const Footer = () => (
  <footer className="border-t border-white/10 bg-[#050816]/80 py-10 text-center text-xs uppercase tracking-[0.3em] text-white/50">
    © {new Date().getFullYear()} NeoCasino · Conceptual experience — no real wagers.
  </footer>
);

export default Footer;
